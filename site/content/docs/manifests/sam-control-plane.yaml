# Copyright 2026 Google LLC
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

apiVersion: v1
kind: Secret
metadata:
  name: sam-control-plane-secret
type: Opaque
stringData:
  SAM_OIDC_ISSUER: "http://mock-oidc:18080"
  # Authenticates the policy admin API. Replace before using this outside a demo.
  SAM_ADMIN_TOKEN: "replace-me-with-a-generated-token"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sam-control-plane
  labels:
    app: sam-control-plane
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sam-control-plane
  template:
    metadata:
      labels:
        app: sam-control-plane
    spec:
      containers:
      - name: sam-control-plane
        image: sam-control-plane:local
        imagePullPolicy: IfNotPresent
        args:
        - "--bind-address=0.0.0.0:8080"
        - "--db-driver=sqlite"
        - "--db-dsn=/data/control-plane.db"
        - "--issuer=$(SAM_OIDC_ISSUER)"
        - "--allowed-audiences=sam-mesh-audience,sam-control-plane-audience"
        - "--insecure-skip-tls-verify"
        ports:
        - containerPort: 8080
          name: http
        env:
        - name: SAM_OIDC_ISSUER
          valueFrom:
            secretKeyRef:
              name: sam-control-plane-secret
              key: SAM_OIDC_ISSUER
        - name: SAM_ADMIN_TOKEN
          valueFrom:
            secretKeyRef:
              name: sam-control-plane-secret
              key: SAM_ADMIN_TOKEN
        volumeMounts:
        - name: db-data
          mountPath: /data
      volumes:
      - name: db-data
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: sam-control-plane
spec:
  type: ClusterIP
  selector:
    app: sam-control-plane
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
    name: http
---
# The control plane starts default closed: it has no policy until one is posted.
# Seed it over the admin API, which is the only way policy is ever set.
#
# SECURITY: a role names what its members may call; a binding names who they
# are. The special member "sam:system:authenticated" matches EVERY identity
# the configured OIDC issuer will vouch for — bound to a wildcard role it
# hands every tool on every node to anyone who can obtain a token from that
# issuer. Never ship that combination. Grant explicit services to explicit
# members, as below.
apiVersion: batch/v1
kind: Job
metadata:
  name: sam-control-plane-seed-policy
spec:
  template:
    spec:
      restartPolicy: OnFailure
      containers:
      - name: seed
        image: curlimages/curl:8.6.0
        env:
        - name: SAM_ADMIN_TOKEN
          valueFrom:
            secretKeyRef:
              name: sam-control-plane-secret
              key: SAM_ADMIN_TOKEN
        command:
        - sh
        - -c
        - |
          set -eu
          until curl -sf -o /dev/null http://sam-control-plane:8080/healthz; do sleep 2; done
          curl -sf -X POST \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${SAM_ADMIN_TOKEN}" \
            -d '{
              "roles": [
                {"name": "demo-caller", "allowed_services": ["mcp://calculator", "system://sam.catalog"], "allowed_targets": ["user:test-user"]}
              ],
              "bindings": [
                {"role": "demo-caller", "members": ["user:test-user"]},
                {"role": "sam:role:node", "members": ["user:test-user"]}
              ]
            }' \
            http://sam-control-plane:8080/policies
